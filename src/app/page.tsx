
/*The NestJS server would be serving a dual role
  Will serve the pages at /
  Will serve trpc endpoint off of /api/trpc/{trpc verb}
*/

export default function Home() {
  return <main className="msx-w-3xl max-auto mt-5"></main>
}
